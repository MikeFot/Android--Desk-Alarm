<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="AppBaseTheme" parent="Theme.AppCompat.NoActionBar">
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <item name="theme_dependent_icon">@drawable/ic_action_settings_light</item>
        <item name="theme_dependent_layout_color">@color/background_holo_dark</item>
    </style>

    <!-- Application theme. -->
    <style name="AppLightTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="theme_dependent_icon">@drawable/ic_action_settings_dark</item>
        <item name="theme_dependent_layout_color">@color/background_holo_light</item>
    </style>

    <style name="DigitalClockTextView" parent="android:Widget.TextView">
        <item name="android:textSize">30sp</item>
        <item name="android:layout_weight">1</item>
        <item name="android:ellipsize">none</item>
        <item name="android:singleLine">true</item>
        <item name="android:scrollHorizontally">false</item>
        <item name="android:layout_width">fill_parent</item>
        <item name="android:layout_height">fill_parent</item>
    </style>

    <style name="AppTheme.AppCompatAlertDialogStyle" parent="Theme.AppCompat.Light.Dialog.Alert">
    </style>

</resources>